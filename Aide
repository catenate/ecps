Dump /git/ecps/dump
ls|mc
osd git add bash sh-inferno
osd git commit -a -m 'add commands to list a project''s steps (and path to step''s command), and a workflow''s states and transitions'
osd git diff 98d0eaf
osd git log --decorate --graph --oneline --all
osd git push origin master
osd git status
|sort|uniq
